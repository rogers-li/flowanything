// Package schema defines runtime-neutral input, output, and context field
// contracts.
//
// It is intentionally independent from config storage, UI, and concrete
// runtimes so server-side and edge/mobile agents can share the same schema
// semantics.
package schema
